import { Routes } from '@angular/router';
import { UnscheduledListingsContainer } from './unscheduled-listings.container';

export const routes: Routes = [
  {
    path: '',
    component: UnscheduledListingsContainer
  }
];
