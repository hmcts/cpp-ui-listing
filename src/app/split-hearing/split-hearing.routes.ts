import { SplitHearingContainer } from './split-hearing.container';
import { Routes } from '@angular/router';

export const routes: Routes = [
  {
    path: '',
    component: SplitHearingContainer
  }
];
