import { EditAllocationContainer } from './edit-allocation.container';
import { Routes } from '@angular/router';

export const routes: Routes = [
  {
    path: '',
    component: EditAllocationContainer
  }
];
