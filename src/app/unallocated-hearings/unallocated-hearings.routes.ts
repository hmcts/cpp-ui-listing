import { Routes } from '@angular/router';
import { UnallocatedHearingsContainer } from './unallocated-hearings.container';

export const routes: Routes = [
  {
    path: '',
    component: UnallocatedHearingsContainer,
  },
];
